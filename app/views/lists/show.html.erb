<h1><%= @list.name %></h1>

<ol>
  <% @bookmarks.each do |bookmark| %>
    <li>
        <%= bookmark.movie.title %>
        <%= bookmark.comment %>
    </li>
  <% end %>
</ol>

<%= link_to 'Add a Movie', new_list_bookmark_path(@list) %>
